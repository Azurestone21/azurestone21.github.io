---
title: 原型和原型链
date: 2023-10-19 21:18:03
categories: [JavaScript]
tags: [JavaScript, 原型, 原型链, 面试题]
---

**前情**

`__proto__` 是浏览器对 `[[prototype]]`属性的实现，也就是`__proto__` 和`[[prototype]]`是一样的。

`__proto__` 和 `constructor` 属性是**对象**所独有的。

`prototype` 属性是**函数**所独有的。但函数也是一种对象，所以函数也拥有`__proto__`和`constructor`属性。

`prototype` 包含所有实例共享的属性和方法，让函数所实例化的对象都可以找到公用的属性和方法。

**`constructor`**属性**指向该对象的构造函数**。

## 原型

在JavaScript里面，每个构造出来的**对象**都有一个隐式原型 `__proto__` ，它指向**构建函数或构建类**的显式原型 `protptype`。

```javascript
function Person() {}
let p = new Person();
```

`p.__proto__` == `Person.prototype`

## 原型链

当访问一个对象的属性时，如果该对象内部不存在这个属性，那么就会去它的`__proto__`属性所指向的那个对象（可以理解为父对象）里找，如果找不到则继续往上找….直到原型链顶端**null**。

这种通过 `__proto__` 属性来连接对象的链式结构被称为原型链 (prototype chain)。

原型链解释了为何一个对象会拥有定义在其他对象中的属性和方法。实例对象会继承构造函数的属性个方法。

![](https://azurestone21.oss-cn-guangzhou.aliyuncs.com/blogs/20260319214629831.png)

## 判断属性或方法是对象本身的

使用 `hasOwnProperty()`

## 总结

原型：函数都有prototype属性，称之为原型，也称为原型对象

- 原型可以放一些属性和方法，共享给实例对象使用
- 原型可以做继承

原型链：对象都有`__proto__`属性，这个属性指向它的原型对象，原型对象也是对象，也有`__proto__`属性，指向原型对象的原型对象，这样一层一层形成的链式结构称为原型链，最顶层找不到则返回 null。
